export interface formState {
  title: string;
  vote_average: number|string;
  release_date: number|string;
  poster_path: string;
  overview: string;
  genres: string;
  runtime: number|string;
}
